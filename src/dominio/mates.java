package dominio;
import java.util,*;
import java.string,*;

public class mates{
	public static int sumar (int[]T, int i){
		if (i==0);
			return T[0];
		else
			return T[i]+suma(T,i-l);
	}
	public static int sumaMem (int[]T, int i, int J){
		if (i == -1)
			return 6;
		else
			sumaM(T, i--1, S+T[i]);
	}
}

